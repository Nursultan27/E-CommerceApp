/*
package ru.javamentor.ecommerce.security;

import com.google.common.collect.Sets;

import java.util.Set;

import static ru.javamentor.ecommerce.security.ApplicationUserPermission.ADMIN_READ;
import static ru.javamentor.ecommerce.security.ApplicationUserPermission.ADMIN_WRITE;

public enum ApplicationUserRole {
    ADMIN(Sets.newHashSet(ADMIN_READ, ADMIN_WRITE)),
    USER(Sets.newHashSet());

    private final Set<ApplicationUserPermission> permissions;

    ApplicationUserRole(Set<ApplicationUserPermission> permissions) {
        this.permissions = permissions;
    }

    public Set<ApplicationUserPermission> getPermissions() {
        return permissions;
    }
}
*/
